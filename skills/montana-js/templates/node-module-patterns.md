# Node module patterns (montana-js)

## Defaults
- Prefer ESM for new modules when repo allows it.
- Avoid mixing ESM and CJS in the same package unless the repo already does.
- Prefer explicit exports; avoid magic side-effect imports unless necessary.

