# Minimal clarifying question set

Ask only what’s required to avoid wrong changes.

- What’s the exact error output (copy/paste) and the command that produced it?
- What is the expected behavior vs actual behavior?
- Which environment(s) matter (OS, Node/Python/Go/Rust versions)?
- Is this a hotfix or can we refactor if needed?

