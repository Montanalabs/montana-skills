# Example output (montana-intake)

**Stack**
- TypeScript + Vite
- Node tooling: npm

**Key folders**
- `src/` — application code
- `public/` — static assets

**Commands**
- Install: `npm ci`
- Lint: `npm run lint`
- Test: `npm run test`
- Build: `npm run build`
- Dev: `npm run dev`

**Next steps**
- Reproduce the issue with `npm run test` (or the smallest failing test)
- Identify the minimal files involved
- Make a small fix and re-run the focused test

**Questions (if blocked)**
- What’s the exact error output (copy/paste)?

