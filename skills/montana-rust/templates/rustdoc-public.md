## rustdoc template for public items (montana-rust)

```rust
/// One-line summary in the imperative mood.
///
/// More detail if needed: invariants, edge cases, and examples.
///
/// # Errors
/// Returns an error if the input is invalid or the operation fails.
pub fn do_thing(/* ... */) -> Result<(), MyError> {
    // ...
}
```

