# OpenClaw adapter note

OpenClaw is the runtime most directly aligned with the current `package/skills/*/SKILL.md` structure.

Use the canonical skills directly unless you need:

- marketplace-specific metadata extensions
- OpenClaw-only install metadata
- target-specific gating for binaries or secrets

